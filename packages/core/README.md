# @orch-os/core

Shared **Zod** schemas, **job state machine** helpers, and **`job-envelope.schema.json`** (generated on build).

```bash
npm run build
```

Schema output: `dist/job-envelope.schema.json` (for Python `orch-plugin validate`).
